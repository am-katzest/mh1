\begin{table}[H]
	\begin{minipage}{0.40\textwidth}
		\resizebox{\textwidth}{!}{
			\begin{tabular}{c|c|c|c|c|c}
				{Pokolenie} & min & Q1 & Q2 & Q3 & max \\
				\hline
				<%= tabela %>
			\end{tabular}
		}
		\caption{\small rozkład maksimum względem pokolenia, dane:
			populacja=<%= population-size %>,
			tempo wymiany=<%= replacement-rate %>,
			ilość uruchomień=<%= runs %>,
			długość=<%= duration %>,
			duplikaty=<%= (name merge-identical) %>,
			funkcja dopasowania=<%= (pretty scoring-fn) %>,
			algorytm wybierania=<%= (pretty distribution-fn) %>,
			prawdopodobieństwo mutacji=<%= (pretty choose-cross-method) %>
		}
	\end{minipage}
	\begin{minipage}{0.60\textwidth}
		\includegraphics[width=\textwidth]{<%= graf %>}
		\label{tab:niepewności}
	\end{minipage}
\end{table}
